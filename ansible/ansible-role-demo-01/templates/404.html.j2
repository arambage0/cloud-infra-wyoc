<!DOCTYPE html>
<html>
<head>
    <title>404 Not Found - {{ ansible_hostname }}</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; padding: 50px; }
        h1 { color: #d9534f; }
    </style>
</head>
<body>
    <h1>404 - Page Not Found</h1>
    <p>Sorry, the page you're looking for doesn't exist on <strong>{{ ansible_hostname }}</strong>.</p>
    <p>Server Time: {{ ansible_date_time.iso8601 }}</p>
    <p>IP Address: {{ ansible_default_ipv4.address }}</p>
    
    {% if ansible_distribution == "Rocky" %}
        <p><em>Powered by Rocky Linux</em></p>
    {% endif %}
</body>
</html>
